[pytest]
testpaths = tests
markers =
    integration: tests that require a live PostgreSQL database
# The Windows system Temp directory is locked on some local environments.
# Keep only pytest-owned artifacts in this ignored project directory instead.
addopts = -ra -p no:cacheprovider --basetemp=.pytest_tmp/session
